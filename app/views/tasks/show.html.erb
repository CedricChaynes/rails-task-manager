<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<div>
  <input type="checkbox" <%= @task.completed == true ? 'checked' : '' %> >
  <label for="scales"><%= @task.completed == true ? 'This task is completed' : 'This task is not completed yet' %></label>
</div>

<p><%= link_to 'Go back to index', tasks_path() %></p>
